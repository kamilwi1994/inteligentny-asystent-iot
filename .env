# --- KONFIGURACJA SIECI DOCKER ---
# Nazwa sieci, w której działają kontenery (musi być taka sama jak u Profesora/Ollamy)
AI_NETWORK_NAME=ai_network

# --- KONFIGURACJA OLLAMA (LLM) ---
# Adres API Ollamy. Jeśli Ollama to inny kontener w tej samej sieci, wpisz http://nazwa_kontenera:11434
OLLAMA_BASE_URL=http://ollama:11434

# Modele do użycia (zostaną pobrane automatycznie jeśli ich nie ma)
OLLAMA_MODEL_CHAT=llama3.2
OLLAMA_MODEL_EMBED=nomic-embed-text

# --- KONFIGURACJA BAZY DANYCH ---
POSTGRES_HOST=db
POSTGRES_DB=home_assistant_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password123

# --- KONFIGURACJA APLIKACJI ---
# Ile dni wstecz generować/analizować?
HISTORY_DAYS=30
# Ile fragmentów (dni) analizować w jednym zapytaniu? (3-5 dla CPU, więcej dla GPU)
RAG_K_RETRIEVAL=30